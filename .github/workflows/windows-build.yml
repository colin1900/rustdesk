name: Build-Windows-Single-EXE

on:
  push:
    branches: [ master, main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-2022
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4
        with:
          submodules: 'recursive'
          fetch-depth: 0

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: stable
          target: x86_64-pc-windows-msvc

      - name: Install Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.24.0'

      - name: Install Build Tools
        run: |
          choco install llvm nasm ninja 7zip -y
          pip install requests

      - name: Setup VCPKG
        shell: powershell
        run: |
          & C:\vcpkg\vcpkg.exe install ffmpeg[avcodec,avformat,swresample,swscale] libvpx libyuv opus aom --triplet x64-windows-static --classic
          echo "LIBCLANG_PATH=C:\Program Files\LLVM\bin" >> $env:GITHUB_ENV
          echo "VCPKG_ROOT=C:\vcpkg" >> $env:GITHUB_ENV

      - name: Prepare Flutter & FRB Codegen
        shell: powershell
        run: |
          cd flutter
          flutter pub get
          cd ..
          # 动态安装匹配版本的 codegen
          $pubspec = Get-Content flutter/pubspec.yaml -Raw
          if ($pubspec -match 'flutter_rust_bridge:\s*[\^~]?([0-9\.]+)') {
              $frbVersion = $Matches[1]
              cargo install flutter_rust_bridge_codegen --version $frbVersion
          }
          flutter_rust_bridge_codegen --rust-input src/flutter_ffi.rs --rust-output src/bridge_generated.rs --dart-output flutter/lib/generated_bridge.dart

          # 修复 LNK2005 符号冲突
          $rustBridge = "src/bridge_generated.rs"
          if (Test-Path $rustBridge) {
              $c = Get-Content $rustBridge -Raw
              $conflicts = @("store_dart_post_cobject","get_dart_object","drop_dart_object","new_dart_opaque","init_frb_dart_api_dl")
              foreach ($fn in $conflicts) { $c = $c -replace "(?s)#\[no_mangle\]\s+pub\s+unsafe\s+extern\s+`"C`"\s+fn\s+$fn\s*\(.*?\)\s*->?\s*.*?\s*\{.*?\}", "/* Removed $fn */" }
              $c = $c -replace 'Dart_Handle', 'isize'
              [System.IO.File]::WriteAllText($rustBridge, $c, [System.Text.Encoding]::UTF8)
          }

      - name: Patch Flutter UI (Compatibility)
        shell: powershell
        run: |
          $flutterDir = "$pwd/flutter"
          # 创建兼容性补丁
          $compat = "import 'package:flutter/material.dart';`nextension ThemeDataCompat on ThemeData { dynamic get TabBarThemeData => this.tabBarTheme; dynamic get DialogThemeData => this.dialogTheme; }"
          $compat | Out-File -FilePath "$flutterDir/lib/flutter_324_compat.dart" -Encoding UTF8
          
          # 注入补丁到 common.dart 和 main.dart
          foreach ($file in @("lib/common.dart", "lib/main.dart")) {
              $path = Join-Path $flutterDir $file
              if (Test-Path $path) {
                  $c = Get-Content $path -Raw
                  if ($c -notmatch 'flutter_324_compat.dart') { $c = "import 'flutter_324_compat.dart';`n" + $c }
                  [System.IO.File]::WriteAllText($path, $c, [System.Text.Encoding]::UTF8)
              }
          }
          cd flutter
          flutter pub run build_runner build --delete-conflicting-outputs

      - name: Build & Package Single EXE
        shell: powershell
        run: |
          # 1. 编译核心
          $sdkVer = (Get-ChildItem "C:\Program Files (x86)\Windows Kits\10\Include\10.*" | Sort-Object Name -Descending | Select-Object -First 1).Name
          $env:BINDGEN_EXTRA_CLANG_ARGS = "-I`"C:\Program Files (x86)\Windows Kits\10\Include\$sdkVer\ucrt`" -I`"C:\vcpkg\installed\x64-windows-static\include`""
          python build.py --flutter
          
          cd flutter
          flutter build windows --release --no-tree-shake-icons
          cd ..

          # 2. 准备 7z SFX 封包
          $releaseDir = "flutter/build/windows/x64/runner/Release"
          $publishDir = "publish"
          New-Item -ItemType Directory -Force $publishDir
          Copy-Item -Path "$releaseDir/*" -Destination $publishDir -Recurse

          # 创建 7z 配置文件 (指定启动程序)
          $sfxConfig = ";!@Install@!UTF-8!`nTitle=`"RustDesk Portable`"`nRunProgram=`"rustdesk.exe`"`n;!@InstallEnd@!"
          $sfxConfig | Out-File -FilePath "sfx_config.txt" -Encoding UTF8

          # 压缩所有文件到 app.7z
          & "7z" a "app.7z" "./$publishDir/*"

          # 下载 7z SFX 模块并合并
          Invoke-WebRequest -Uri "https://github.com/mcmilk/7-Zip-Scripts/raw/master/RunProgram/x64/7zSD.sfx" -OutFile "7zSD.sfx"
          
          # 合并为单文件
          cmd /c "copy /b 7zSD.sfx + sfx_config.txt + app.7z RustDesk-Portable.exe"

      - name: Upload Single EXE
        uses: actions/upload-artifact@v4
        with:
          name: RustDesk-Single-EXE
          path: RustDesk-Portable.exe
